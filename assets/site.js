const input=document.getElementById('searchInput'),grid=document.getElementById('essayGrid'),cards=[...(grid?.querySelectorAll('.essay-card')||[])],filters=[...document.querySelectorAll('.filter-chip')],sort=document.getElementById('sortSelect'),empty=document.getElementById('emptyState'),count=document.getElementById('visibleCount');let activeTag='';const norm=s=>(s||'').toLocaleLowerCase('pt-BR').normalize('NFD').replace(/\p{Diacritic}/gu,'');function apply(){const q=norm(input?.value.trim());let vis=[];for(const c of cards){const hit=(!q||norm(c.dataset.search).includes(q))&&(!activeTag||(c.dataset.tags||'').split('|').includes(activeTag));c.hidden=!hit;if(hit)vis.push(c)}vis.sort((a,b)=>sort?.value==='title'?a.dataset.title.localeCompare(b.dataset.title,'pt-BR'):(b.dataset.updated||'').localeCompare(a.dataset.updated||''));vis.forEach(c=>grid.appendChild(c));if(count)count.textContent=vis.length;if(empty)empty.hidden=vis.length!==0}input?.addEventListener('input',apply);sort?.addEventListener('change',apply);filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');activeTag=b.dataset.tag||'';apply()}));document.addEventListener('keydown',e=>{if(e.key==='/'&&document.activeElement?.tagName!=='INPUT'){e.preventDefault();input?.focus()}});apply();
